/** @type {import('tailwindcss').Config} */
export default {
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
  theme: {
    extend: {
      colors: {
        // ── 뉴트럴 ────────────────────────────────────────────────
        // 배경이 크림(#FBF7F1)이라 순수 회색을 쓰면 차갑고 탁하게 뜬다.
        // 따뜻한 기가 도는 뉴트럴이라야 크림과 붙는다.
        //
        // 본문 텍스트는 전부 여기서 가져온다. 예전에는 text-brand-dark/50 처럼
        // 브라운에 투명도를 먹여 썼는데, 그러면 화면의 모든 글자가 갈색조가 되어
        // 브랜드 컬러가 아무 의미도 갖지 못한다. 브라운은 브랜드를 말할 때만 쓴다.
        //
        // 대비를 재보고 전부 진하게 내렸다. 소상공인 사용자는 연령대가 높아 옅은 회색이
        // 특히 불리하다. 텍스트로 쓰는 셋은 크림·흰카드 어디에 얹어도 AA(4.5:1)를 넘긴다.
        //
        //                     크림 배경   흰 카드
        ink: '#1C1917', //      16.4:1     17.5:1   제목·본문
        muted: '#57534E', //     7.2:1      7.6:1   보조 설명
        subtle: '#6E6862', //    5.2:1      5.5:1   라벨·캡션
        //
        // 아래는 '텍스트가 아닌 것' 전용이다. 비활성 날짜, 빈 상태 아이콘 같은 장식.
        // 여기에 본문을 얹으면 안 된다.
        faint: '#A8A29E', //     2.4:1  ← 텍스트 금지
        line: '#E7E5E4', //  구분선·테두리

        // ── 브랜드 ────────────────────────────────────────────────
        brand: {
          DEFAULT: '#8B5E1C', // 로고, 강조 텍스트
          dark: '#6F4A12',
          light: '#B5915A',
        },

        // ── Primary CTA ───────────────────────────────────────────
        // 기존 버튼(#6F4A12)은 채도가 낮은 진흙색이라 무겁고 올드했다.
        // 밝기가 아니라 '채도'를 올려야 생기가 생긴다.
        // accent 오렌지와 같은 계열을 어둡게 내린 색이라 팔레트가 자연스럽게 이어진다.
        //
        // 흰 글자 대비 5.22:1 — WCAG AA 통과.
        // (accent 오렌지 #F5A623은 흰 글자 대비가 2.04:1이라 버튼 채움색으로 못 쓴다)
        primary: {
          DEFAULT: '#C2410C',
          hover: '#9A3412',
          soft: '#FFF1EA', // 연한 배경 (선택 상태 등)
        },

        // ── 액센트 ────────────────────────────────────────────────
        // 활성 탭, 저장됨 표시. 채움색이 아니라 '표시'용.
        accent: {
          DEFAULT: '#F5A623',
          soft: '#FCE9CC',
        },

        cream: '#FBF7F1',

        // ── 상태 ──────────────────────────────────────────────────
        // 빨강은 '마감 임박'에만 쓴다. 초록·파랑을 남발하면 정작 빨강이 안 보인다.
        status: {
          red: '#DC2626', // 마감 임박 (D-7 이내) — 유일한 빨강
          green: '#15803D', // 상시 접수 (나쁜 소식이 아니라 좋은 소식이다)
          blue: '#4C7DF0',
        },
      },
      boxShadow: {
        // 그림자를 갈색이 아니라 뉴트럴로. 갈색 그림자는 화면을 탁하게 만든다.
        card: '0 1px 3px rgba(41, 37, 36, 0.04), 0 1px 2px rgba(41, 37, 36, 0.03)',
        lift: '0 4px 16px rgba(41, 37, 36, 0.08)',
      },
      fontFamily: {
        sans: ['Pretendard', 'system-ui', 'sans-serif'],
      },
      fontSize: {
        // 타이포 위계. 예전에는 로고와 페이지 타이틀이 거의 같은 크기라 서로 싸웠고,
        // 웨이트가 bold/semibold 둘뿐이라 단계가 없었다.
        title: ['26px', { lineHeight: '1.3', fontWeight: '700' }], // 페이지 타이틀
        section: ['17px', { lineHeight: '1.4', fontWeight: '700' }], // 섹션 제목
        card: ['15px', { lineHeight: '1.45', fontWeight: '600' }], // 카드 제목
      },
    },
  },
  plugins: [],
}
